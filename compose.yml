services:
  db:
    image: postgres:17
    container_name: database
    environment:
      - POSTGRES_USER=intro
      - POSTGRES_PASSWORD=contraseña_segura
      - POSTGRES_DB=adopciones
    ports:
      - 5433:5432
    volumes:
      - ./psql_dat3a:/var/lib/postgresql/data